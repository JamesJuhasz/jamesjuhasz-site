import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "About" };

export default function AboutPage() {
  return (
    <ComingSoon
      title="About"
      day="Day 2"
      blurb="The full story page — editorial hero, the journey from the Great Lakes to Malta, career timeline — lands with Day 2 of the migration."
    />
  );
}
