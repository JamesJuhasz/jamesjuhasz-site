import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "Events" };

export default function EventsPage() {
  return (
    <ComingSoon
      title="Events"
      day="Day 3"
      blurb="Race log + training calendar. 27 events from the Squarespace backup, plus upcoming regattas."
    />
  );
}
