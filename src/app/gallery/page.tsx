import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "Gallery" };

export default function GalleryPage() {
  return (
    <ComingSoon
      title="Gallery"
      day="Day 3"
      blurb="13 sub-galleries from across the campaign, with a masonry lightbox treatment."
    />
  );
}
