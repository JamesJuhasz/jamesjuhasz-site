import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "Newsletters" };

export default function NewslettersPage() {
  return (
    <ComingSoon
      title="Newsletters"
      day="Day 5"
      blurb="20 posts from the road — imported from the Squarespace export and wired to Sanity CMS."
    />
  );
}
