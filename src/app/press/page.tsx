import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "Press" };

export default function PressPage() {
  return (
    <ComingSoon
      title="Press"
      day="Day 3"
      blurb="Media mentions and recognition — authority signals to support the campaign."
    />
  );
}
